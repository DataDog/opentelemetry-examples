clusterName: ""
defaultCRConfig:
  presets:
    resourceDetection:
      aks:
        enabled: true
collectors:
  daemon:
    presets:
      resourceDetection:
        aks:
          enabled: true
instrumentation:
  resource:
    resourceAttributes:
      deployment.environment.name: "production"