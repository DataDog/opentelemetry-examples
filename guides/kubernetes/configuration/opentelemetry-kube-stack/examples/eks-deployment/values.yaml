clusterName: ""
defaultCRConfig:
  presets:
    resourceDetection:
      eks:
        enabled: true
collectors:
  daemon:
    presets:
      resourceDetection:
        aks:
          enabled: false
        eks:
          enabled: true
instrumentation:
  resource:
    resourceAttributes:
      deployment.environment.name: "production"