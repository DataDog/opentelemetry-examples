clusterName: ""
defaultCRConfig:
  presets:
    resourceDetection:
      gcp:
        enabled: true
collectors:
  daemon:
    presets:
      resourceDetection:
        gcp:
          enabled: true
instrumentation:
  resource:
    resourceAttributes:
      deployment.environment.name: "production"